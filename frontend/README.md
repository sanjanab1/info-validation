
  # Chat web app code

  ## Running the code

  Run `npm i` to install the dependencies.

  To enable Microsoft Clarity, add `VITE_CLARITY_PROJECT_ID` to your local environment or deployment environment with the Clarity project ID from the Clarity dashboard.

  Run `npm run dev` to start the development server.
  