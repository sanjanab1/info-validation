
  # Chat web app code

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

## How to access data
Open DevTools console and run:

`JSON.parse(localStorage.getItem('iv_analytics'))`

Or visit: `/analytics-9k2m`